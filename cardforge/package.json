{
  "name": "cardsmith-studio",
  "private": true,
  "version": "0.1.0",
  "description": "CardSmith Studio: Windows-first desktop app for designing and exporting tabletop/card-game components.",
  "scripts": {
    "dev": "concurrently -n UI,MAIN -c auto \"npm run dev:renderer\" \"npm run dev:main\"",
    "dev:main": "wait-on tcp:5173 && npm --prefix apps/main run dev",
    "dev:renderer": "npm --prefix apps/renderer run dev",
    "build": "npm --prefix packages/core run build && npm --prefix packages/storage run build && npm --prefix apps/renderer run build && npm --prefix apps/main run build",
    "dist": "npm run build && npm --prefix apps/main run dist",
    "lint": "npm --prefix apps/renderer run lint",
    "test": "npm --prefix packages/core run test",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "prettier": "^3.4.2",
    "wait-on": "^7.2.0"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
